#include "Point.hpp"


bool bsp(Point const a, Point const b, Point const c, Point const point)
{

}
// a b c point du triangles
// point le point a verifier
//return: true si le point est dans le triangle, sinon faux
// ainsi si le point est sur un coter ou un sommet c'est faux








// pour chaque segment je dois faire un produit vectoriel
// si le resultat est positif ou negatif tu sais si le point est 
// dans le triangle
// wikipedia -> calcul d'aire
// si les trois aires sont egals a l'aire total du triangles (nouveau point)
// si une des aires vaut 0 alors point sur l'une d'elle
// faire les calculs puis appeler 3 fois la fonctions
// utiliser geogebra